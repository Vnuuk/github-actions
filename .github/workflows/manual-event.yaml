name: API Trigger

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Name"
        required: true
        type: string

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Show Name
        run: echo "${{ inputs.name }}"
